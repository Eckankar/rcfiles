servers = (
  { address = "irc.ubuntu.com"; chatnet = "Ubuntu"; port = "6667"; },
  { address = "irc.stealth.net"; chatnet = "IRCnet"; port = "6668"; },
  { address = "irc.efnet.org"; chatnet = "EFNet"; port = "6667"; },
  { 
    address = "irc.undernet.org";
    chatnet = "Undernet";
    port = "6667";
  },
  { address = "irc.dal.net"; chatnet = "DALnet"; port = "6667"; },
  { 
    address = "irc.quakenet.org";
    chatnet = "QuakeNet";
    port = "6667";
  },
  { address = "silc.silcnet.org"; chatnet = "SILC"; port = "706"; },
  {
    address = "chat.eu.freenode.net";
    chatnet = "freenode";
    port = "6667";
    use_ssl = "no";
    ssl_verify = "no";
    autoconnect = "yes";
  },
  {
    address = "holmes.freenode.net";
    chatnet = "freenode";
    port = "6667";
  },
  {
    address = "gibson.freenode.net";
    chatnet = "freenode";
    port = "6667";
  },
  {
    address = "irc.synirc.net";
    chatnet = "SynIRC";
    port = "6667";
    use_ssl = "no";
    ssl_verify = "no";
    autoconnect = "yes";
  },
  {
    address = "irc.psigenix.net";
    chatnet = "PSIGenix";
    port = "6667";
    use_ssl = "no";
    ssl_verify = "no";
    autoconnect = "no";
  },
  {
    address = "irc.quickfox.net";
    chatnet = "QuickFox";
    port = "6667";
    use_ssl = "no";
    ssl_verify = "no";
    autoconnect = "yes";
  },
  {
    address = "eu.undernet.org";
    chatnet = "Undernet";
    port = "6667";
    use_ssl = "no";
    ssl_verify = "no";
    autoconnect = "yes";
  },
  {
    address = "irc.inet.tele.dk";
    chatnet = "EFNet";
    port = "6667";
    use_ssl = "no";
    ssl_verify = "no";
  },
  {
    address = "Hamburg.DE.EU.undernet.org";
    chatnet = "Undernet";
    port = "6667";
    use_ssl = "no";
    ssl_verify = "no";
  },
  {
    address = "irc.perl.org";
    chatnet = "Perl";
    port = "6667";
    use_ssl = "no";
    ssl_verify = "no";
    autoconnect = "yes";
  }
);

chatnets = {
  IRCnet = {
    type = "IRC";
    max_kicks = "4";
    max_msgs = "5";
    max_whois = "4";
    max_query_chans = "5";
  };
  EFNet = { 
    type = "IRC";
    max_kicks = "4";
    max_msgs = "3";
    max_whois = "1";
  };
  Undernet = {
    type = "IRC";
    max_kicks = "1";
    max_msgs = "3";
    max_whois = "30";
  };
  DALnet = {
    type = "IRC";
    max_kicks = "4";
    max_msgs = "3";
    max_whois = "30";
  };
  QuakeNet = {
    type = "IRC";
    max_kicks = "1";
    max_msgs = "3";
    max_whois = "30";
  };
  SILC = { type = "SILC"; };
  Ubuntu = { type = "IRC"; };
  freenode = { type = "IRC"; nick = "Sebbe"; };
  SynIRC = { type = "IRC"; nick = "Sebbe"; };
  PSIGenix = { type = "IRC"; nick = "Sebbe"; };
  QuickFox = { type = "IRC"; nick = "Sebbe"; };
  Perl = { type = "IRC"; nick = "Sebbe"; };
};

channels = (
  { name = "#diku"; chatnet = "freenode"; autojoin = "yes"; },
  { name = "#eggsml"; chatnet = "freenode"; autojoin = "yes"; },
  { 
    name = "#eggsml-maskinrum";
    chatnet = "freenode";
    autojoin = "yes";
  },
  { name = "#dikurevy"; chatnet = "freenode"; autojoin = "yes"; },
  { name = "#nethack"; chatnet = "freenode"; autojoin = "no"; },
  { name = "#quickfox"; chatnet = "QuickFox"; autojoin = "yes"; },
  { name = "#psystra"; chatnet = "QuickFox"; autojoin = "yes"; },
  { name = "#regex"; chatnet = "Undernet"; autojoin = "yes"; },
  { name = "#mojo"; chatnet = "Perl"; autojoin = "yes"; },
  { name = "#p5p"; chatnet = "Perl"; autojoin = "yes"; },
  { name = "#cph.pm"; chatnet = "Perl"; autojoin = "yes"; }
);

aliases = {
  J = "join";
  WJOIN = "join -window";
  WQUERY = "query -window";
  LEAVE = "part";
  BYE = "quit";
  EXIT = "quit";
  SIGNOFF = "quit";
  DESCRIBE = "action";
  DATE = "time";
  HOST = "userhost";
  LAST = "lastlog";
  SAY = "msg *";
  WW = "whowas";
  W = "who";
  N = "names";
  M = "msg";
  T = "topic";
  C = "clear";
  CL = "clear";
  K = "kick";
  KB = "kickban";
  KN = "knockout";
  BANS = "ban";
  B = "ban";
  IG = "ignore";
  UNIG = "unignore";
  SB = "scrollback";
  UMODE = "mode $N";
  WC = "window close";
  WN = "window new hide";
  GOTO = "sb goto";
  CHAT = "dcc chat";
  RUN = "SCRIPT LOAD";
  SBAR = "STATUSBAR";
  INVITELIST = "mode $C +I";
  Q = "QUERY";
  "MANUAL-WINDOWS" = "set use_status_window off;set autocreate_windows off;set autocreate_query_level none;set autoclose_windows off;set reuse_unused_windows on;save";
  EXEMPTLIST = "mode $C +e";
  ATAG = "WINDOW SERVER";
  USJOVT = "say concieggs: det er jo ikke sjovt";
  SJOVT = "say concieggs: det er faktisk ret sjovt";
  USJOV = "say concieggs: det er jo ikke sjovt";
  SJOV = "say concieggs: det er faktisk ret sjovt";
};

statusbar = {
  # formats:
  # when using {templates}, the template is shown only if it's argument isn't
  # empty unless no argument is given. for example {sb} is printed always,
  # but {sb $T} is printed only if $T isn't empty.

  items = {
    # start/end text in statusbars
    barstart = "{sbstart}";
    barend = "{sbend}";

    topicbarstart = "{topicsbstart}";
    topicbarend = "{topicsbend}";

    # treated "normally", you could change the time/user name to whatever
    time = "{sb $Z}";
    user = "{sb {sbnickmode $cumode}$N{sbmode $usermode}{sbaway $A}}";

    # treated specially .. window is printed with non-empty windows,
    # window_empty is printed with empty windows
    window = "{sb $winref:$tag/$itemname{sbmode $M}}";
    window_empty = "{sb $winref{sbservertag $tag}}";
    prompt = "{prompt $[.15]itemname}";
    prompt_empty = "{prompt $winname}";
    topic = " $topic";
    topic_empty = " Irssi v$J - http://irssi.org/help/";

    # all of these treated specially, they're only displayed when needed
    lag = "{sb Lag: $0-}";
    act = "{sb Act: $0-}";
    more = "-- more --";
  };

  # there's two type of statusbars. root statusbars are either at the top
  # of the screen or at the bottom of the screen. window statusbars are at
  # the top/bottom of each split window in screen.
  default = {
    # the "default statusbar" to be displayed at the bottom of the window.
    # contains all the normal items.
    window = {
      disabled = "no";

      # window, root
      type = "window";
      # top, bottom
      placement = "bottom";
      # number
      position = "1";
      # active, inactive, always
      visible = "active";

      # list of items in statusbar in the display order
      items = {
        barstart = { priority = "100"; };
        time = { };
        user = { };
        window = { };
        window_empty = { };
        lag = { priority = "-1"; };
        act = { priority = "10"; };
        more = { priority = "-1"; alignment = "right"; };
        barend = { priority = "100"; alignment = "right"; };
      };
    };

    # statusbar to use in inactive split windows
    window_inact = {
      type = "window";
      placement = "bottom";
      position = "1";
      visible = "inactive";
      items = {
        barstart = { priority = "100"; };
        window = { };
        window_empty = { };
        more = { priority = "-1"; alignment = "right"; };
        barend = { priority = "100"; alignment = "right"; };
      };
    };

    # we treat input line as yet another statusbar :) It's possible to
    # add other items before or after the input line item.
    prompt = {
      type = "root";
      placement = "bottom";
      # we want to be at the bottom always
      position = "100";
      visible = "always";
      items = {
        prompt = { priority = "-1"; };
        prompt_empty = { priority = "-1"; };
        # treated specially, this is the real input line.
        input = { priority = "10"; };
      };
    };

    # topicbar
    topic = {
      type = "root";
      placement = "top";
      position = "1";
      visible = "always";
      items = {
        topicbarstart = { priority = "100"; };
        topic = { };
        topic_empty = { };
        topicbarend = { priority = "100"; alignment = "right"; };
      };
    };
  };
};
settings = {
  core = {
    real_name = "Sebastian P.";
    user_name = "sebbe";
    nick = "sebbe";
    recode_autodetect_utf8 = "yes";
  };
  "fe-text" = { 
    actlist_sort = "refnum";
    autostick_split_windows = "yes";
  };
  "fe-common/core" = {
    autolog = "yes";
    autolog_path = "~/irclogs/%Y/$tag/$0.%m-%d.log";
    window_history = "yes";
    beep_when_window_active = "yes";
    beep_when_away = "yes";
    beep_msg_level = "MSGS NOTICES DCC DCCMSGS HILIGHT";
    bell_beeps = "yes";
    term_charset = "utf-8";
  };
  "perl/core/scripts" = {
    hilightwin_showprivmsg = "no";
    nicklist_width = "16";
    nicklist_automode = "SCREEN";
  };
};
ignores = (
  {
    level = "CRAP MSGS NOTICES SNOTES CTCPS JOINS PARTS QUITS KICKS MODES TOPICS WALLOPS INVITES NICKS DCC DCCMSGS CLIENTNOTICES CLIENTCRAP CLIENTERRORS";
    channels = ( "#letsplay" );
  },
  {
    level = "CRAP MSGS NOTICES SNOTES CTCPS JOINS PARTS QUITS KICKS MODES TOPICS WALLOPS INVITES NICKS DCC DCCMSGS CLIENTNOTICES CLIENTCRAP CLIENTERRORS";
    channels = ( "#cobol" );
  },
  {
    level = "CRAP MSGS NOTICES SNOTES CTCPS JOINS PARTS QUITS KICKS MODES TOPICS WALLOPS INVITES NICKS DCC DCCMSGS CLIENTNOTICES CLIENTCRAP CLIENTERRORS";
    channels = ( "#nethack" );
  }
);
hilights = (
  { text = "Sebbe"; nick = "yes"; word = "yes"; },
  {
    text = "GOGDeals!*@*";
    nick = "yes";
    word = "yes";
    mask = "yes";
    channels = ( "#gogdeals" );
  }
);
logs = { };
windows = {
  1 = {
    immortal = "yes";
    name = "(status)";
    level = "ALL";
    sticky = "yes";
    parent = "3";
  };
  2 = {
    items = (
      {
        type = "CHANNEL";
        chat_type = "IRC";
        name = "#diku";
        tag = "freenode";
      }
    );
    sticky = "yes";
    parent = "3";
  };
  3 = {
    items = (
      {
        type = "CHANNEL";
        chat_type = "IRC";
        name = "#eggsml";
        tag = "freenode";
      }
    );
    sticky = "yes";
  };
  4 = {
    items = (
      {
        type = "CHANNEL";
        chat_type = "IRC";
        name = "#dikurevy";
        tag = "freenode";
      }
    );
    sticky = "yes";
    parent = "3";
  };
  5 = {
    items = (
      {
        type = "CHANNEL";
        chat_type = "IRC";
        name = "#eggsml-maskinrum";
        tag = "freenode";
      }
    );
    sticky = "yes";
    parent = "3";
  };
  6 = {
    items = (
      {
        type = "CHANNEL";
        chat_type = "IRC";
        name = "#quickfox";
        tag = "QuickFox";
      }
    );
    sticky = "yes";
    parent = "3";
  };
  7 = {
    items = (
      {
        type = "CHANNEL";
        chat_type = "IRC";
        name = "#psystra";
        tag = "QuickFox";
      }
    );
    sticky = "yes";
    parent = "3";
  };
  8 = {
    items = (
      {
        type = "CHANNEL";
        chat_type = "IRC";
        name = "#regex";
        tag = "Undernet";
      }
    );
    sticky = "yes";
    parent = "3";
  };
  20 = { name = "hilight"; sticky = "yes"; };
};
mainwindows = {
  3 = { first_line = "8"; lines = "52"; };
  20 = { first_line = "1"; lines = "7"; };
};
